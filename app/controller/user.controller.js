import BaseController from '../core/base.class'

class UserController extends BaseController {
  async index () {
    const { ctx, logger } = this

  }

  async login () {
    const { ctx, logger } = this

    return {}
  }
}

export default UserController
